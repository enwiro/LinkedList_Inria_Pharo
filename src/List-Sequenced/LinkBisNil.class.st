"
LinkBisNil is there to avoid isNil and exception catches in LinkedListBis. It doesn't do anything but it is useful.
"
Class {
	#name : #LinkBisNil,
	#superclass : #Object,
	#category : #'List-Sequenced-Support'
}

{ #category : #'change set' }
LinkBisNil >> setNext: aLinkBis [ 
	
]
